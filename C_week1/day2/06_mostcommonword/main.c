#include <stdio.h>
#include <ctype.h>

int main()
{
    FILE * working_file = fopen("file.txt", "r");
    

    for(int i = 0; str[i]; i++){
        str[i] = tolower(str[i]);
    }
    printf("Hello, World!\n");
    return 0;
}